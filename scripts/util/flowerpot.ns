import { performGrow } from "/util/util.ns";

/** @param {NS} ns
 *  Arguments are 'target', 'threads'
 *  **/
export async function main(ns) {
	ns.disableLog("ALL");
	return await performGrow(ns, ns.args[0], ns.args[1]);
}